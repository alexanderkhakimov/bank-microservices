apiVersion: v1
kind: Service
metadata:
  name: {{ .Chart.Name }}
  namespace: {{ .Values.global.namespace }}
spec:
  type: ClusterIP
  ports:
  - port: {{ .Values.global.ports.transferService }}
    targetPort: {{ .Values.global.ports.transferService }}
  selector:
    app: {{ .Chart.Name }}

    