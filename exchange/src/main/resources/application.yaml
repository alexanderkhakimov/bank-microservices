server:
  port: 8086
spring:
  application:
    name: exchange-service
  datasource:
    url: jdbc:mysql://localhost:3306/studyDB?useSSL=false&serverTimezone=UTC
    username: root
    password: 19931012
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
    show-sql: true
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8180/realms/bank
  keycloak:
    realm: bank
    auth-server-url: http://localhost:8180
    resource: exchange-service
    credentials:
      secret: 9kElCaWDTjcaaWfyjuKItCmyJfdsipgt