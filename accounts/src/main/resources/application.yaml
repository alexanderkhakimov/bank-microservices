server:
  port: 8082

spring:
  application:
    name: accounts

  config:
    import: "optional:consul:"

  cloud:
    consul:
      host: localhost
      port: 8500
      discovery:
        enabled: true
        health-check-path: /actuator/health
        service-name: accounts

  datasource:
    url: jdbc:mysql://localhost:3306/studyDB?useSSL=false&serverTimezone=UTC
    username: root
    password: 19931012
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
    show-sql: true
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8180/realms/bank
  keycloak:
    realm: bank
    auth-server-url: http://localhost:8180
    resource: accounts
    credentials:
      secret: oflL9KgT18jd7iRwLDBFIFDymfqQriM6