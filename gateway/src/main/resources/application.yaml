spring:
  application:
    name: api-gateway
  config:
    import: "optional:consul:"

  cloud:
    consul:
      host: localhost
      port: 8500
      discovery:
        enabled: true
        health-check-path: /actuator/health
        service-name: api-gateway

    gateway:
      discovery:
        locator:
          enabled: true
          lower-case-service-id: true

      routes:
        - id: root-route  # Добавлен дефис для элемента массива
          uri: no://op
          predicates:
            - Path=/
          filters:
            - RedirectTo=302, /login
        - id: front-ui-route  # Добавлен дефис для элемента массива
          uri: lb://front-ui
          order: 10000
          predicates:
            - Path=/**
          filters:
            - TokenRelay

        - id: accounts-service
          uri: lb://accounts
          predicates:
            - Path=/api/accounts/**
          filters:
            - TokenRelay

server:
  port: 8090
  reactive:
    session:
      cookie:
        name: SESSION_${spring.application.name}

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always

logging:
  level:
    org.springframework.cloud.gateway: DEBUG
    org.springframework.security: DEBUG
    org.springframework.cloud.consul: INFO